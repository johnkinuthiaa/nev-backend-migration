package com.slippery.nevmigration.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface NearBySchoolsRepository extends JpaRepository<NearBySchoolsRepository,Long> {
}
